import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF0D0D1A);
  static const surface = Color(0xFF16162A);
  static const surfaceVariant = Color(0xFF1E1E35);

  // Brand palette
  static const primary = Color(0xFF7C3AED); // Purple
  static const primaryContainer = Color(0xFF2D1B69);
  static const secondary = Color(0xFF06B6D4); // Cyan
  static const secondaryContainer = Color(0xFF0E3A47);
  static const tertiary = Color(0xFFFACC15); // Yellow/Gold
  static const tertiaryContainer = Color(0xFF3D3200);

  // Status
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF22C55E);

  // On-colors
  static const onBackground = Color(0xFFF0F0FF);
  static const onSurface = Color(0xFFB0B0D0);
  static const onPrimary = Color(0xFFFFFFFF);
  static const onSecondary = Color(0xFF000000);
  static const onTertiary = Color(0xFF000000);

  // Neo-brutalism
  static const border = Color(0xFFFFFFFF);
  static const borderMuted = Color(0xFF3A3A5C);
}
